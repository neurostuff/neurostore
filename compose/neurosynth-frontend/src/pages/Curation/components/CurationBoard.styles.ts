import { Style } from 'index';

const CurationBoardStyles: Style = {
    columnContainer: {
        width: '100%',
        height: '100%',
        display: 'flex',
        overflowX: 'auto',
    },
};

export default CurationBoardStyles;
