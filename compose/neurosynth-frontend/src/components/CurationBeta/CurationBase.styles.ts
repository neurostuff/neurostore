import { Style } from 'index';

const CurationBaseStyles: Style = {
    tabStyles: {
        fontSize: '1rem',
        fontWeight: 'bold',
        ':hover': {
            color: 'orange',
        },
    },
};

export default CurationBaseStyles;
