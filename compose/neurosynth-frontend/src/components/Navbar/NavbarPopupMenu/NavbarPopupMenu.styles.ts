import { Style } from 'index';

const NavbarPopupMenuStyles: Style = {
    link: {
        textDecoration: 'none',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
    },
};

export default NavbarPopupMenuStyles;
