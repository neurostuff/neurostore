import { render } from '@testing-library/react';
import NeurosynthTable from './NeurosynthTable';

describe('Neurosynth Table Component', () => {
    it('should render', () => {
        // render(<NeurosynthTable tableConfig={{} as any} headerCells={[]} />);
        expect(true).toBeTruthy();
    });
});
