import { Style } from '../../..';

const NavbarPopupMenuStyles: Style = {
    link: {
        textDecoration: 'none',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        color: 'primary.contrastText',
    },
};

export default NavbarPopupMenuStyles;
