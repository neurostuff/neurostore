import { Box } from '@mui/material';

const PublicMetaAnalysesPage: React.FC = (props) => {
    return <Box>place holder for the public meta analyses page</Box>;
};

export default PublicMetaAnalysesPage;
