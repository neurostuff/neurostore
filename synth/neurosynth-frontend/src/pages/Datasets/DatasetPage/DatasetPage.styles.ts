import { Style } from '../../..';

const DatasetPageStyles: Style = {
    noData: {
        color: 'warning.dark',
    },
    displayedText: {
        margin: 'auto 0',
    },
};

export default DatasetPageStyles;
