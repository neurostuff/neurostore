import { INavbarPopupMenu } from '../NavbarPopupMenu';

const mockNavbarPopupMenu: React.FC<INavbarPopupMenu> = (props) => {
    return <button>child-menuitem</button>;
};

export default mockNavbarPopupMenu;
