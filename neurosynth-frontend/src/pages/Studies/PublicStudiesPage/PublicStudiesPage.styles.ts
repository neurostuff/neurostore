import { Style } from '../../..';

const PublicStudiesPageStyles: Style = {
    paginator: {
        display: 'flex',
        justifyContent: 'end',
    },
};

export default PublicStudiesPageStyles;
