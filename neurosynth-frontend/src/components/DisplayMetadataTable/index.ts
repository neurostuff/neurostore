/**
 * file that handles display metadata table models
 */

export interface DisplayMetadataTableModel {
    metadata: any;
}

export interface DisplayMetadataTableRowModel {
    metadataKey: string;
    metadataValue: any;
}
