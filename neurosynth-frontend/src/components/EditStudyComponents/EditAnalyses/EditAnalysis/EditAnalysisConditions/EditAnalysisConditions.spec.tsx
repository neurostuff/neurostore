import { render } from '@testing-library/react';
import EditAnalysisConditions from './EditAnalysisConditions';

/**
 * Placeholder test
 */

describe('EditAnalysisConditions Component', () => {
    it('should render', () => {
        render(<EditAnalysisConditions />);
        expect(true).toBe(true);
    });
});
